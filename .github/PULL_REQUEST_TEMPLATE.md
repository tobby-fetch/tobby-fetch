## Summary

<!-- What does this change do, and why? -->

## Requirements touched

<!-- List SRS requirement IDs (e.g. FR-035, NFR-016) or ADRs this PR
implements, changes, or is constrained by. Write "None" if not applicable. -->

## Checklist

- [ ] Tests added or updated, and `mise run test` passes locally
- [ ] `mise run lint` passes (strict profile, zero suppressions)
- [ ] Every commit is signed off (`git commit -s`) — DCO
- [ ] User-facing strings have both EN and FR i18n keys (UI changes only)
- [ ] Documentation updated (README, `docs/`, or inline docs) if behavior
      changed
