ok   scenario m4 started 2026-08-18T14:36:50Z
ok   tooling available (helm v4.2.3+g43e8b7f, cosign present, openssl, jq)
ok   built tobby and the forward proxy for linux/amd64
ok   instances launched (source, destination, proxy, node, sealed node)
ok   addresses assigned (source 10.180.10.27, dest 10.180.10.171, proxy 10.180.10.23, node 10.180.10.177, sealed 10.180.30.23)
ok   private PKI issued (root + leaf for 10.180.10.171)
ok   fixtures serving: production registry, TLS zone registry (private CA), forward proxy
ok   cookbook published and signed by the real cosign (1.0.0 → sha256:e010ffa5d3349c6b41d8282c21da743fed0b9f37b863adc9634dde197a56e9f4, 1.0.1 → sha256:b0233d50e5ea19918523a48818b5dc993a3bd196c941b88fef2e45f7649fa0c1)
ok   node deployed from the reference chart (rendered config, chart-managed roots, hardening asserted)
ok   secure by default (401 RFC 9457 + Basic challenge); the override opens access with its permanent banner
ok   cycle 1: recipe verified and promoted, zone cookbook published, pinned digest intact at the destination
ok   cycle 2: nothing pushed, every destination item already up to date (FR-026, FR-028)
ok   FR-013: interval changed at runtime, a scheduled cycle fired, the override survived a restart, the change was audited
ok   FR-030: off-list destination refused before transfer, audited, counted; the refused recipe never landed
ok   sealed node: no direct route to either registry, and the proxy refuses anonymous egress (407)
ok   blocked egress: fetched and promoted entirely through the authenticated proxy, over private-PKI TLS
ok   blocked egress, cycle 2: nothing pushed — idempotent behind the proxy too
ok   scenario m4 complete — report: crucible-report-m4.txt

# Acceptance host: disposable Scaleway Elastic Metal (cheapest hourly offer in
# stock, fr-par-2, EM-A116X-SSD, Intel Xeon E3-1230 v3, 8 cores, 31 GiB, Ubuntu
# 24.04.3, kernel 6.8.0-88, Incus 6.0/apt, dir pool, helm 4.2.3 and cosign 3.x
# installed on the host), created 2026-08-18 13:37 UTC, destroyed 14:39 UTC
# after report retrieval (ADR-0014 disposable-host profile; system containers
# only, no KVM needed). Two hourly windows billed, ~0.15 EUR total across both
# acceptance sessions.
#
# The m1, m2 and m3 scenarios were replayed green on the same host beforehand,
# from the same commit: the milestone-4 engine changes touch the paths all
# three exercise. Their reports are milestone-1- through
# milestone-3-crucible-report.txt, and every report above comes from the
# committed tree — nothing was patched on the node for the run that produced
# them.
#
# Three defects were found by these runs and fixed before this report, none of
# them visible to any other tier of the pyramid:
#
#   B-014  Trust scopes were matched against two different pattern spaces on
#          the two halves of a promotion. On a registry carrying a port, a
#          correctly written scope admitted a recipe at import and refused it
#          before the push.
#   B-015  A Sigstore bundle signature — cosign 3.x's default — verified on the
#          zone that fetched it and was absent one hop down, because the copy
#          only ever learned the tag-attached layout. The zone below refused
#          content its upstream had accepted.
#   (scenario) The m3 fixtures derived their 2.0.1 and 2.0.2 versions with a
#          substitution that also moved the ingredient's version, so the
#          cascade step looked for content under a tag the source never had.
#
# The first two are product defects that shipped in v0.3.0 and would have
# reached a zone. Both survived that long because the crucible step that
# exercises them — the cosign 3.x bundle layout — was added to m3 AFTER
# milestone 3 was accepted, and had never run in front of the cascade.
#
# Known limitation, stated rather than hidden: "deployed from the reference
# chart" means the chart is rendered and its configuration and security context
# are what the instance runs. No kubelet is involved at this tier; a real
# cluster run belongs to the RKE2 crucible node of a later milestone.
